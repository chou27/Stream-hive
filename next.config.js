// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   experimental: {
//     serverActions: true,
//   },
// };

module.exports = nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No experimental features if not needed
};

module.exports = nextConfig;

// module.exports = {
//   reactStrictMode: true,
//   experimental: {
//     serverActions: true,
//   },
//   runtime: 'node', // Ensure you're using the Node.js runtime
// };

